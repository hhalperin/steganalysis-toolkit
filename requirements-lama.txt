# Optional: LaMa inpainting for texture-aware watermark removal
# Install with: pip install -r requirements-lama.txt
# Note: May have dependency conflicts with Pillow>=10; use a separate env if needed.
simple-lama-inpainting>=0.1.2
torch>=2.13.0
torchvision>=0.14.1
